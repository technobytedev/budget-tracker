import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.isubol.budgettracker',
  appName: 'Isubol Budget Tracker',
  webDir: 'dist'
};

export default config;
