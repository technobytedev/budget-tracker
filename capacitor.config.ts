import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Isubol Budget Tracker',
  webDir: 'dist'
};

export default config;
